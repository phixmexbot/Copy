document.addEventListener('DOMContentLoaded', () => {
  const openButton = document.querySelector('#openButton');
  const sendMessage = document.querySelector('#sendButton');

  openButton.addEventListener('click', () => {
    console.log('hello');
  });
});
